# Project name
name: 'dbt_project'
version: '1.0.0'

profile: 'dbt_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets: 
  - "target"
  - "dbt_packages"


models:
  dbt_project:
    +materialized: view 

    staging:
      +materialized: view
      +schema: liquor_silver

    intermediate:
      +materialized: view
      +schema: liquor_silver

    marts:
      +schema: liquor_gold
      +materialized: table 
      
      reporting:
        +materialized: view
